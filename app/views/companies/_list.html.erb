
<% @companies.each_slice(4) do |row| %>
	<div class="row">
		<% row.each do |venture| %>
			<div class="3u venture-box">
				<%= image_tag venture.logo_url, class: 'image bropic' %>
			</div>
		<% end %>
	</div>
	 <span class="stretch" > </span>
<% end %>