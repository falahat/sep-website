<html class="homepage">
	<%= render 'shared/header' %>
	<div class="wrapper style2">
		<section class="container">
			<header class="major">
				<h2> <%= @title %> </h2>
			</header>
			<%= render 'image_list' %>
		</section>
	<%= render "shared/footer" %>
</html>